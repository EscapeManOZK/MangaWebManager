import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.patoupoland.fr',
  appName: 'manga-manager',
  webDir: 'dist/manga-web-manager',
  bundledWebRuntime: false
};

export default config;
